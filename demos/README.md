# Scopify demo

`demo/` is a single, self-contained mini-project that illustrates **every**
Scopify rule (`SC001`-`SC003`, `SC010`-`SC018`) with explanatory comments
next to each offending line. See `demo/README.md` for the full rule-by-rule
tour and instructions to run the checker against it.
